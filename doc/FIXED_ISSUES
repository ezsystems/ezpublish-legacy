A list of fixed issues:
----------------------

Link XML TAG
-----------
* Store links in link database ( ezlink ). Make upgrade script which will convert
  all links stored without this reference to correct.

eZ Setup System
-----------------------------------------------

* Fix problems with languages not being set correctly, create some common functions
  which extracts settings from the persistence list and any post variables and use
  those functions in the different steps.

* Check if file uploading is enabled, if not give clues on how to enabled it.
  See php.ini file_uploads = Off

* Make sure the demo site is activated, instead of the admin, if demo data is installed.

* Add mysql socket to db init page.
