{* DO NOT EDIT THIS FILE! Use an override template instead. *}
{def $error=$persistent_data.error}
{* DESIGN: Header START *}<div class="box-header"><div class="box-tc"><div class="box-ml"><div class="box-mr"><div class="box-tl"><div class="box-tr">
<h1 class="context-title">
{'Installing package'|i18n('design/standard/package')} '{$package.name}'.
</h1>
{* DESIGN: Mainline *}<div class="header-mainline"></div>
{* DESIGN: Header END *}</div></div></div></div></div></div>

{* DESIGN: Content START *}<div class="box-ml"><div class="box-mr"><div class="box-content">

<div class="context-attributes">

{def $path=concat( 'package/install/', $package.name )}
<form method="post" action={$path|ezurl}>
{if is_set($error.description)}
    <h3>{$error.description}</h3>
    {if is_set($error.actions)}
        <h4>{'Please choose action:'|i18n('design/standard/package')|break}</h4>
        <div class="optionslist">
        {def $i=0}
        {foreach $error.actions as $action_id => $action}
            <input class="radiobutton" id="{$action_id}" type="radio" name="ActionID" value="{$action_id}" {if eq($i, 0)}checked="checked"{/if} />{$action|wash}<br />
            {set $i=inc($i)}
        {/foreach}
        </div>
        <br />
        <label for="RememberAction"><input class="checkbox" id="RememberAction" type="checkbox" name="RememberAction"/>{'Use this choice for all the items'|i18n('design/standard/package')}</label>
    {else}
        <p>Element with ID '{$error.element_id}' will not be installed.</p>
    {/if}
{else}
    <h3>{'Unhandled installation error has occurred.'|i18n('design/standard/package')}</h3>
{/if}
</div>
{* DESIGN: Content END *}</div></div></div>

<div class="controlbar">
{* DESIGN: Control bar START *}<div class="box-bc"><div class="box-ml"><div class="box-mr"><div class="box-tc"><div class="box-bl"><div class="box-br">
    <div class="block">
        <input class="button" type="submit" name="HandleError" value="{'Continue'|i18n('design/standard/package')}" />
        <input class="button" type="submit" name="SkipPackageButton" value="{'Cancel installation'|i18n('design/standard/package')}" />
    </div>
{* DESIGN: Control bar END *}</div></div></div></div></div></div>
</div>
</form>
