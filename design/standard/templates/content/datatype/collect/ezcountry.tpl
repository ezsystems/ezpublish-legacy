{* DO NOT EDIT THIS FILE! Use an override template instead. *}
{default attribute_base=ContentObjectAttribute}
{let countries=fetch( 'content', 'country_list' )
     class_content=$attribute.class_content}

{def $country = cond( is_set( $#collection_attributes[$attribute.id] ), $#collection_attributes[$attribute.id], $attribute.content.value )}
<select name="{$attribute_base}_country_{$attribute.id}[]" {if $class_content.multiple_choice}multiple="multiple"{/if}>
    {if $class_content.multiple_choice|not}
        <option  value="">{'Not specified'|i18n( 'design/standard/content/datatype' )}</option>
    {/if}	
{foreach $countries as $key => $current_country}
    {if $country|ne( '' )}
        {if $country|is_array|not}
            {* Backwards compatability *}
            <option {if $country|eq( $current_country )}selected="selected"{/if} value="{$key}">{$current_country}</option>
        {else}
            <option {if is_set( $country.$key )}selected="selected"{/if} value="{$key}">{$current_country}</option>
        {/if}
    {else}
            <option {if is_set( $class_content.default_countries.$key )}selected="selected"{/if} value="{$key}">{$current_country}</option>
    {/if}
{/foreach}
</select>

{/let}
{/default}
