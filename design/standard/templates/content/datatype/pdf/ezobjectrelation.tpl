{* DO NOT EDIT THIS FILE! Use an override template instead. *}
{if and( $attribute.content, or( $attribute.content.can_read, $attribute.content.can_view_embed ), $attribute.content.has_visible_node )}
    {pdf(
        link,
        hash(
            url, concat( 'content/view/full/', $attribute.content.visible_nodes[0].node_id )|ezurl( 'no' ),
            text, $attribute.content.name|wash( 'pdf' )
        )
    )};
{else}
   {pdf(text, "No relation"|i18n("design/standard/content/datatype")|wash(pdf))}
{/if}