{* DO NOT EDIT THIS FILE! Use an override template instead. *}
<form action={"/content/removeassignment/"|ezurl} method="post">

<h1>{'Removal of locations'|i18n( 'design/standard/location' )}</h1>

<p>{'Some of the locations you tried to remove has children, are you really sure you want to remove those locations?
If you do all the children will be removed as well.'|i18n( 'design/standard/location' )}</p>

<table class="list">
<tr>
    <th>{'Path'|i18n( 'design/standard/location' )}</th>
    <th>{'Count'|i18n( 'design/standard/location' )}</th>
</tr>
{section var=remove_item loop=$remove_list sequence=array( bglight, bgdark )}
<tr class="{$remove_item.sequence}">
    <td>{$remove_item.node.object.class_identifier|class_icon( small, $remove_item.node.object.class_name )}{section var=path_node loop=$remove_item.node.path}{$path_node.name|wash}{delimiter} / {/delimiter}{/section}</td>
    <td>{if $remove_item.count|eq( 1 )}{$remove_item.count} child{else}{$remove_item.count} children{/if}</td>
</tr>
{/section}
</table>

<input type="submit" class="button" name="ConfirmRemovalButton" value="{'Remove locations'|i18n( 'design/standard/location' )}" />
<input type="submit" class="button" name="CancelRemovalButton" value="{'Cancel removal'|i18n( 'design/standard/location' )}" />

</form>
